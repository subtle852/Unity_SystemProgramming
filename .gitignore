# =========================================================
# 1. Unity 엔진 자동 생성 및 캐시 파일 (필수 제외)
#    - 이 파일들은 각 개발자 환경에서 로컬로 재생성되며, 커밋 시 충돌을 유발합니다.
# =========================================================

# Library 폴더: 에셋 임포트 메타데이터, 캐시 파일, 플랫폼별 데이터가 포함됩니다.
# (프로젝트에서 가장 크며, Git에 절대 올리면 안 되는 파일이 모여있습니다.)
Library/

# Temp 폴더: 컴파일 및 에셋 처리 과정에서 유니티가 생성하는 임시 파일입니다.
Temp/

# Obj 폴더: IDE(통합 개발 환경)가 컴파일 과정에서 생성하는 중간 오브젝트 파일입니다.
Obj/

# .vs 폴더: 솔루션 개인 사용자 설정 및 캐시 파일입니다.
.vs/

# Logs 폴더: 빌드된 게임이 실행되는 동안 발생하는 진단 및 오류 기록 파일입니다.
Logs/

# =========================================================
# 2. 빌드 및 실행 결과 파일 (제외)
#    - 최종 결과물로, 코드가 아니며 Git 저장소 용량을 크게 차지합니다.
# =========================================================

# Builds 폴더: 빌드 결과물이 저장되는 폴더입니다.
Builds/

# 플랫폼별 실행 파일 및 설치 패키지 파일
# (Android, iOS, PC, WebGL 등 모든 플랫폼의 빌드 결과물을 제외합니다.)
*.apk        # Android Application Package
*.aab        # Android App Bundle
*.xapk
*.ipa        # iOS App Archive
*.exe        # Windows 실행 파일
*.dmg        # macOS 디스크 이미지
*.app/       # macOS 애플리케이션 번들 (폴더 형태로 인식)
*.xcdetour
*.xctest
*.xcode
*.app.dSYM   # 디버그 심볼 파일
*.pdb        # 디버그 심볼 파일 (Windows)
*.psym
*.js         # WebGL 빌드 파일
*.html       # WebGL 빌드 파일
*.wasm       # WebAssembly 파일 (WebIL)

# =========================================================
# 3. IDE 및 로컬 프로젝트 설정 파일 (충돌 방지 목적)
#    - 개발자 개인의 작업 환경 설정 파일로, 공유되면 안 됩니다.
# =========================================================

# Visual Studio 및 Rider 솔루션/프로젝트 파일
# (각자의 환경에서 재생성되므로 제외합니다.)
*.csproj
*.unityproj
*.sln        # 솔루션 파일
*.suo        # 솔루션 유저 옵션 파일
*.user
*.userprefs
*.pidb
*.booproj
*.nitobi
*.njsproj
*.wixproj
*.vspscc
*.vssscc
*.tmp
*.vcproj
*.iws
*.vshost.exe
*.vshost.exe.config
*.webinfo

# Visual Studio 및 Rider의 숨김 폴더
.vscode/     # Visual Studio Code 로컬 설정 폴더
.vs/         # Visual Studio 로컬 설정 폴더
.idea/       # JetBrains Rider 로컬 설정 폴더

# Unity 에디터 레이아웃 및 기타 유저 설정 파일
*.EditorUserSettings.asset # 에디터 레이아웃 설정
Rider/                     # Rider 관련 사용자별 설정 폴더

# =========================================================
# 4. OS 및 디버그, 임시 파일 (제외)
# =========================================================

# macOS 관련 시스템 파일
.DS_Store          # 폴더 보기 설정 파일
.Spotlight-V100
.Trashes
__MACOSX

# Windows 관련 시스템 파일
Thumbs.db          # 썸네일 캐시 파일
desktop.ini

# 디버거 및 프로파일러 파일
*.mdb              # Mono 디버그 파일
*.opendb
*.crash            # 충돌 로그 파일

# 컴파일된 코드/캐시 및 인덱싱 파일
*.bin              # 바이너리 파일
*.vsidx            # Visual Studio Code/IDE 인덱싱 파일

# =========================================================
# 5. 필수 포함 파일 (제외 목록에 없음을 확인)
# =========================================================
# 아래 파일들은 Unity 프로젝트 실행에 필수적이므로, .gitignore에 포함하지 않습니다.
# 1) Assets/ 폴더 (모든 코드, 씬, 에셋 파일)
# 2) ProjectSettings/ 폴더 (프로젝트 전체 설정)
# 3) Packages/ 폴더 (패키지 매니저 파일)
# 4) 모든 *.meta 파일 (에셋의 GUID 및 설정)
# =========================================================

# =========================================================